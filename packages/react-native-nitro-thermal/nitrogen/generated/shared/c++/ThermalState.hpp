///
/// ThermalState.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/NitroHash.hpp>)
#include <NitroModules/NitroHash.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/JSIConverter.hpp>)
#include <NitroModules/JSIConverter.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/NitroDefines.hpp>)
#include <NitroModules/NitroDefines.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

namespace margelo::nitro::nitrothermal {

  /**
   * An enum which can be represented as a JavaScript union (ThermalState).
   */
  enum class ThermalState {
    NOMINAL      SWIFT_NAME(nominal) = 0,
    FAIR      SWIFT_NAME(fair) = 1,
    SERIOUS      SWIFT_NAME(serious) = 2,
    CRITICAL      SWIFT_NAME(critical) = 3,
    UNKNOWN      SWIFT_NAME(unknown) = 4,
  } CLOSED_ENUM;

} // namespace margelo::nitro::nitrothermal

namespace margelo::nitro {

  // C++ ThermalState <> JS ThermalState (union)
  template <>
  struct JSIConverter<margelo::nitro::nitrothermal::ThermalState> final {
    static inline margelo::nitro::nitrothermal::ThermalState fromJSI(jsi::Runtime& runtime, const jsi::Value& arg) {
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, arg);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("nominal"): return margelo::nitro::nitrothermal::ThermalState::NOMINAL;
        case hashString("fair"): return margelo::nitro::nitrothermal::ThermalState::FAIR;
        case hashString("serious"): return margelo::nitro::nitrothermal::ThermalState::SERIOUS;
        case hashString("critical"): return margelo::nitro::nitrothermal::ThermalState::CRITICAL;
        case hashString("unknown"): return margelo::nitro::nitrothermal::ThermalState::UNKNOWN;
        default: [[unlikely]]
          throw std::invalid_argument("Cannot convert \"" + unionValue + "\" to enum ThermalState - invalid value!");
      }
    }
    static inline jsi::Value toJSI(jsi::Runtime& runtime, margelo::nitro::nitrothermal::ThermalState arg) {
      switch (arg) {
        case margelo::nitro::nitrothermal::ThermalState::NOMINAL: return JSIConverter<std::string>::toJSI(runtime, "nominal");
        case margelo::nitro::nitrothermal::ThermalState::FAIR: return JSIConverter<std::string>::toJSI(runtime, "fair");
        case margelo::nitro::nitrothermal::ThermalState::SERIOUS: return JSIConverter<std::string>::toJSI(runtime, "serious");
        case margelo::nitro::nitrothermal::ThermalState::CRITICAL: return JSIConverter<std::string>::toJSI(runtime, "critical");
        case margelo::nitro::nitrothermal::ThermalState::UNKNOWN: return JSIConverter<std::string>::toJSI(runtime, "unknown");
        default: [[unlikely]]
          throw std::invalid_argument("Cannot convert ThermalState to JS - invalid value: "
                                    + std::to_string(static_cast<int>(arg)) + "!");
      }
    }
    static inline bool canConvert(jsi::Runtime& runtime, const jsi::Value& value) {
      if (!value.isString()) {
        return false;
      }
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, value);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("nominal"):
        case hashString("fair"):
        case hashString("serious"):
        case hashString("critical"):
        case hashString("unknown"):
          return true;
        default:
          return false;
      }
    }
  };

} // namespace margelo::nitro
